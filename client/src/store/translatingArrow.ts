import {createSlice,PayloadAction} from "@reduxjs/toolkit";

const initialState = {

}

const translateState = createSlice({
    name: "translateState",
    initialState,
    reducers:{
        
    }
})